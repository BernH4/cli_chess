class Rook
  def initialize(x, y)
    @curr_cords = [x, y]
  end
end

class Knight
  def initialize(x, y)
    @curr_cords = [x, y]
  end
end

class Bishop
  def initialize(x, y)
    @curr_cords = [x, y]
  end
end

class Queen
  def initialize(x, y)
    @curr_cords = [x, y]
  end
end

class King
  def initialize(x, y)
    @curr_cords = [x, y]
  end
end

class Pawn
  def initialize(x, y)
    @curr_cords = [x, y]
  end
end
